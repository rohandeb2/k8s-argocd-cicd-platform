https://claude.ai/chat/1216a675-e675-45d9-bff0-bf7599b32a6d



ayushmanndeb8@gmail.com


Final deployed by this version
https://claude.ai/chat/abe25666-2051-42f2-adbf-bf5242f31d96
rohancyber8@gmail.com


Perfect setup — you have everything needed. Here's the full deployment in order:

---

## Phase 1 — Start Minikube

```bash
minikube start \
  --driver=docker \
  --cpus=4 \
  --memory=4096 \
  --kubernetes-version=stable \
  --addons=metrics-server \
  --addons=ingress \
  --profile=devops-lab
```

Verify:
```bash
kubectl get nodes
# Should show: devops-lab   Ready
```

---

## Phase 2 — Install ArgoCD

```bash
kubectl create namespace argocd

kubectl apply -n argocd \
  -f https://raw.githubusercontent.com/argoproj/argo-cd/stable/manifests/install.yaml
```

Wait for it to be ready (takes ~2 mins):
```bash
kubectl wait --for=condition=Ready pods \
  -l app.kubernetes.io/name=argocd-server \
  -n argocd \
  --timeout=300s
```

Get the admin password:
```bash
kubectl -n argocd get secret argocd-initial-admin-secret \
  -o jsonpath="{.data.password}" | base64 -d
echo ""
```

**Save that password.**

---

## Phase 3 — GitHub Secrets

Go to your repo → **Settings → Secrets and variables → Actions → New repository secret** and add these 3:

| Secret Name | Value |
|-------------|-------|
| `DOCKERHUB_USERNAME` | `rohan700` |
| `DOCKERHUB_TOKEN` | Get from hub.docker.com → Account Settings → Security → New Access Token |
| `GH_PAT` | Get from github.com → Settings → Developer settings → Fine-grained tokens → your repo → Contents + Workflows (Read & Write) |

---

## Phase 4 — Build and push your first image manually

```bash
cd app
docker login
docker build -t rohan700/training-app:latest .
docker push rohan700/training-app:latest
docker tag rohan700/training-app:latest rohan700/training-app:stable
docker push rohan700/training-app:stable
cd ..
```

---

## Phase 5 — Apply ArgoCD project and apps

```bash
# Create namespace first
kubectl create namespace devops-training

# Apply ArgoCD project (RBAC)
kubectl apply -f argocd/projects/devops-training.yaml

# Apply individual apps
kubectl apply -f argocd/apps/application-dev.yaml
kubectl apply -f argocd/apps/application-staging.yaml
kubectl apply -f argocd/apps/application-prod.yaml
```

Verify ArgoCD sees them:
```bash
kubectl get applications -n argocd
```

---

## Phase 6 — Open ArgoCD UI

```bash
kubectl port-forward svc/argocd-server -n argocd 8080:443 &
```

Open **https://localhost:8080** in your browser → accept the certificate warning → login with `admin` and the password from Phase 2.

You'll see 3 apps: `training-app-dev`, `training-app-staging`, `training-app-prod` — all showing **OutOfSync**.

---

## Phase 7 — Sync dev and staging

```bash
# Install ArgoCD CLI
curl -sSL -o argocd-linux-amd64 \
  https://github.com/argoproj/argo-cd/releases/latest/download/argocd-linux-amd64
sudo install -m 555 argocd-linux-amd64 /usr/local/bin/argocd
rm argocd-linux-amd64

# Login
argocd login localhost:8080 \
  --username admin \
  --password $(kubectl -n argocd get secret argocd-initial-admin-secret -o jsonpath="{.data.password}" | base64 -d) \
  --insecure

# Sync dev
argocd app sync training-app-dev

# Sync staging
argocd app sync training-app-staging
```

Watch pods come up:
```bash
watch kubectl get pods -n devops-training
```

---

## Phase 8 — Access your app

```bash
kubectl port-forward svc/training-app-svc -n devops-training 3001:80 &
```

Open **http://localhost:3001** — you should see:
```json
{
  "message": "Production-grade DevOps Training App",
  "version": "v1.0.0-dev",
  "environment": "dev"
}
```

---

## Phase 9 — Trigger the full CI/CD pipeline

Now make a code change to kick off GitHub Actions:

```bash
# Edit the app message
sed -i 's/Production-grade DevOps Training App/Production-grade DevOps Training App - v2/' app/src/index.js

git add app/src/index.js
git commit -m "feat: update app message to trigger pipeline"
git push
```

Then watch:
1. Go to your GitHub repo → **Actions tab** — you'll see the pipeline running
2. It will run tests → build image → push to Docker Hub → update `k8s/overlays/dev/kustomization.yaml` with new SHA tag → commit back
3. ArgoCD detects the git change → auto-syncs dev and staging
4. Run `watch kubectl get pods -n devops-training` to see the rolling update live

---

## Quick status check at any point

```bash
# All pods
kubectl get pods -n devops-training

# ArgoCD app status
kubectl get applications -n argocd

# App logs
kubectl logs -l app=training-app -n devops-training --tail=20

# HPA status
kubectl get hpa -n devops-training
```

Start with Phase 1 and paste any errors you hit — I'll unblock you immediately.